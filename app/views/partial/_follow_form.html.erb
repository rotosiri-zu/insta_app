<% unless current_user?(user) %>
  <div id="follow_form-<%= user.id %>">
    <% if current_user.following?(user) %>
      <%= render "partial/unfollow", user: user %>
    <% else %>
      <%= render "partial/follow", user: user %>
    <% end %>
  </div>
<% end %>
